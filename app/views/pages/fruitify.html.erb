Welcome to Fruitr.

	<%= form_tag fruitify_path, :method=>:get do  %>
	  <%= text_field "name", params[:name]  %>
  	<%end%>


	<% @result.each do |key, val| %>
		<% key.each do |sum|%>
		<%= val["similarartists"]["artist"].first%>
		<% end %>
	<%end%>